时钟周期：震荡周期，时钟脉冲的倒数。是计算机中最基本的、最小的时间单位。一个时钟周期就是一个节拍。
赫兹：1秒内完成周期性变化的次数
1GHz = 1000MHz = 1000 * 1000KHz = 1000 * 1000 * 1000Hz

机器周期：
一条指令的执行过程划分为若干个阶段，每个阶段完成一项工作，这项工作被称为一个基本操作。完成一个基本操作所需时间。
一个机器周期由若干S周期组成。
8051系列，一个机器由6个S周期组成。
一个S周期为两个节拍（时钟周期）。

指令周期
一条指令所需要的时间，一般由多个机器周期组成。
指令不同所需要的机器周期也不同。
